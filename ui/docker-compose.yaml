services:
    app:
        image: swda-24hs/ui:latest
        build:
            context: .
        networks:
            - backbone
        ports:
            - "8080:80"

networks:
    backbone:
        name: g09-backbone_backbone
        external: true
